'use strict';

import { constants } from 'mylife-tools-ui';

export default constants.wrap({
  SET_OPERATION_VIEW : null,
  SET_CRITERIA       : null,
  SELECT_OPERATIONS  : null,
  SET_DETAIL         : null,
}, 'management');
